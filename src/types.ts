export type FlimApi = {
    title: string;
    descriptions: string;
    release_year: number,
    language_id: number,
    rental_duration: number,
    rental_rate: number,
    length: number,
    replacement_cost: number,
    rating: string,
    special_features: string[],
    fulltext: string,
    categories: string[]
}